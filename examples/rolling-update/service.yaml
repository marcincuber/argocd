apiVersion: v1
kind: Service
metadata:
  name: rolling-update
  labels:
    app.kubernetes.io/name: rolling-update
    app.kubernetes.io/part-of: argocd-local-tutorial
spec:
  selector:
    app.kubernetes.io/name: rolling-update
  ports:
    - name: http
      port: 80
      targetPort: http
