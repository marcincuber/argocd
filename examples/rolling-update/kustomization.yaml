apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - disruption-budget.yaml
  - service.yaml

configMapGenerator:
  - name: rolling-content
    literals:
      - index.html=<h1>Rolling update v1</h1><p>Three replicas keep this page available.</p>
