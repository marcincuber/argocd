apiVersion: v1
kind: Service
metadata:
  name: color-demo
  labels:
    app.kubernetes.io/name: color-demo
    app.kubernetes.io/part-of: argocd-local-tutorial
spec:
  selector:
    app.kubernetes.io/name: color-demo
    tutorial.argocd.io/track: blue
  ports:
    - name: http
      port: 80
      targetPort: http
