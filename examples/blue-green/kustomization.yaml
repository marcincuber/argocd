apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - blue.yaml
  - green.yaml
  - service.yaml

configMapGenerator:
  - name: blue-content
    literals:
      - index.html=<h1>Blue release</h1><p>Managed by Argo CD.</p>
  - name: green-content
    literals:
      - index.html=<h1>Green release</h1><p>Managed by Argo CD.</p>
